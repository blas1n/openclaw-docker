# OpenClaw Docker with Tailscale - Environment Configuration
# Copy this file to .env and fill in your values
# IMPORTANT: Never commit .env to git!

# ============================================================================
# Tailscale Configuration (REQUIRED)
# ============================================================================

# Tailscale Auth Key
# Generate at: https://login.tailscale.com/admin/settings/keys
#
# Instructions:
# 1. Create a new auth key
# 2. Set "Reusable" to Yes (optional, for easier redeployment)
# 3. Add tag: tag:openclaw (recommended for ACL management)
# 4. Set expiration as needed (90 days recommended)
# 5. Copy the generated key (starts with tskey-auth-...)
#
TS_AUTHKEY=tskey-auth-xxx-your-auth-key-here

# ============================================================================
# OpenClaw Configuration
# ============================================================================
#
# All OpenClaw settings are configured in openclaw.json
# See openclaw.json.example for configuration options
#
# To modify OpenClaw settings, edit openclaw.json:
# - Gateway binding (loopback for security)
# - Authentication token
# - Ollama model selection
# - Tool restrictions
# - Session management
# - Security policies
#
